#include <stdio.h>

int main() {

    printf("Hello, World!"); //text in a print function must be encapsulated in ""
    printf("\nShopping List:\nChicken Noodle Soup"); // \n can be used to move text to the next line, while \n \n creates a blank line

    int num = 3;
    printf("%d", num); //printing variables requires format identifiers
    //%d = integer
    //%c = char
    //%f = float


    return 0;
}